const CHOOSE_IMAGE: string = 'CHOOSE_IMAGE';
const CANCEL_CHOSEN_IMAGE: string = 'CANCEL_CHOSEN_IMAGE';
const EDIT_GOOD: string = 'EDIT_GOOD';
const SET_EXPIRY: string = 'SET_EXPIRY';
const CHECK_AVAILABLE: string = 'CHECK_AVAILABLE';
const PICK_LOCATION: string = 'PICK_LOCATION';
const RESET_ALL: string = 'RESET_ALL';

const REQUEST_GOOD: string = 'REQUEST_GOOD';

// To define an action, there is one requirement: each action much have its own type property.

export {
    CHOOSE_IMAGE, CANCEL_CHOSEN_IMAGE, EDIT_GOOD, SET_EXPIRY, CHECK_AVAILABLE, PICK_LOCATION, RESET_ALL,
    REQUEST_GOOD
};
