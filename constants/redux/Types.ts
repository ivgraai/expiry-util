const CHOOSE_IMAGE: string = 'CHOOSE_IMAGE';
const EDIT_GOOD: string = 'EDIT_GOOD';

// To define an action, there is one requirement: each action much have its own type property.

export { CHOOSE_IMAGE, EDIT_GOOD };
