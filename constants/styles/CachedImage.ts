import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    zoomHeaderTouchableOpacity: {
        backgroundColor: "black"
    },
    zoomHeaderIonicons: {
        textAlign: "right",
        paddingRight: 10
    },
    touchableOpacity: {
        flex: 1,
        justifyContent: "center"
    }
});
